#ifndef PICO_BP_BOARD_
#define PICO_BP_BOARD_

#include <string>

enum BoardType: int { Pico, PicoWireless };
static BoardType pico_board_type() {
    return (std::string("@PICO_BOARD@") == "pico") ? Pico : PicoWireless;
}

#endif   // PICO_BP_BOARD_